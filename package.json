{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "author": "Kara Rainey - Following Tutor Ania Kubow via YouTube",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
